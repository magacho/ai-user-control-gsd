---
phase: 01-auth-user-management
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/main/resources/templates/layout/default.html
  - src/main/resources/templates/fragments/sidebar.html
  - src/main/resources/templates/fragments/navbar.html
  - src/main/resources/templates/dashboard.html
  - src/main/resources/templates/users/list.html
  - src/main/resources/templates/users/fragments/table.html
  - src/main/resources/templates/ai-tools/list.html
  - src/main/resources/templates/ai-tools/form.html
  - src/main/resources/templates/ai-tools/fragments/table.html
  - src/main/resources/templates/ai-tools/fragments/form-modal.html
  - src/main/resources/static/css/custom.css
  - src/main/java/com/bemobi/aiusercontrol/web/DashboardController.java
  - src/main/java/com/bemobi/aiusercontrol/user/controller/UserController.java
  - src/main/java/com/bemobi/aiusercontrol/user/service/UserService.java
  - src/main/java/com/bemobi/aiusercontrol/aitool/controller/AIToolController.java
  - src/main/java/com/bemobi/aiusercontrol/aitool/service/AIToolService.java
  - src/main/java/com/bemobi/aiusercontrol/dto/request/AIToolRequest.java
  - src/main/java/com/bemobi/aiusercontrol/dto/response/AIToolResponse.java
  - src/main/java/com/bemobi/aiusercontrol/dto/response/UserResponse.java
  - src/main/java/com/bemobi/aiusercontrol/config/WebConfig.java
autonomous: true
requirements:
  - USER-01

must_haves:
  truths:
    - "Admin sees a left sidebar with navigation items after login"
    - "Admin sees their Google avatar and name in the top navbar"
    - "Admin can view a read-only list of users in a data table"
    - "Admin can create a new AI tool through the dashboard UI"
    - "Admin can edit an existing AI tool through the dashboard UI"
    - "Admin can delete an AI tool from the catalog"
    - "User list shows empty state message when no users exist yet"
    - "AI tool list shows empty state message when no tools configured"
    - "All pages share the same layout with sidebar and navbar"
    - "User list page has no create, edit, or delete buttons -- read-only data table only"
  artifacts:
    - path: "src/main/resources/templates/layout/default.html"
      provides: "Base layout with sidebar + navbar + content area"
      contains: "layout:fragment"
    - path: "src/main/resources/templates/fragments/sidebar.html"
      provides: "Left sidebar navigation with menu items"
      contains: "sidebar"
    - path: "src/main/resources/templates/fragments/navbar.html"
      provides: "Top navbar with Google avatar, user name, logout"
      contains: "navbar"
    - path: "src/main/resources/templates/dashboard.html"
      provides: "Main dashboard page with empty state"
      contains: "layout:decorate"
    - path: "src/main/resources/templates/users/list.html"
      provides: "Read-only user list with data table"
      contains: "layout:decorate"
    - path: "src/main/resources/templates/ai-tools/list.html"
      provides: "AI tool catalog list with CRUD actions"
      contains: "hx-get"
    - path: "src/main/java/com/bemobi/aiusercontrol/web/DashboardController.java"
      provides: "Dashboard and login page routing"
      contains: "class DashboardController"
    - path: "src/main/java/com/bemobi/aiusercontrol/user/controller/UserController.java"
      provides: "User list endpoint (read-only)"
      contains: "class UserController"
    - path: "src/main/java/com/bemobi/aiusercontrol/aitool/controller/AIToolController.java"
      provides: "AI tool CRUD endpoints with HTMX support"
      contains: "@HxRequest"
    - path: "src/main/java/com/bemobi/aiusercontrol/aitool/service/AIToolService.java"
      provides: "AI tool business logic (create, update, delete, list)"
      contains: "class AIToolService"
  key_links:
    - from: "src/main/resources/templates/dashboard.html"
      to: "src/main/resources/templates/layout/default.html"
      via: "Thymeleaf layout decoration"
      pattern: "layout:decorate.*layout/default"
    - from: "src/main/resources/templates/ai-tools/list.html"
      to: "src/main/java/com/bemobi/aiusercontrol/aitool/controller/AIToolController.java"
      via: "HTMX hx-get/hx-post for CRUD operations"
      pattern: "hx-get.*ai-tools"
    - from: "src/main/java/com/bemobi/aiusercontrol/aitool/controller/AIToolController.java"
      to: "src/main/java/com/bemobi/aiusercontrol/aitool/service/AIToolService.java"
      via: "service delegation for CRUD"
      pattern: "aiToolService\\."
    - from: "src/main/resources/templates/fragments/navbar.html"
      to: "OAuth2 user principal"
      via: "Thymeleaf security expressions for avatar and name"
      pattern: "sec:authentication|authentication\\.principal"
---

<objective>
Build the complete UI layer: shared layout with left sidebar navigation and Google avatar navbar, read-only user list, AI tool catalog CRUD with HTMX partial updates, and dashboard landing page.

Purpose: With authentication working from Plan 02, this plan creates everything the admin sees after login. The user list is read-only (users come from provider APIs in later phases) with empty state handling. The AI tool catalog is the only CRUD feature per CONTEXT.md -- admins create/edit/delete tool definitions through the UI.

Output: A fully navigable admin dashboard with sidebar, user list page (empty state), AI tool management with HTMX-powered CRUD.
</objective>

<execution_context>
@/home/flavio.magacho/.claude/get-shit-done/workflows/execute-plan.md
@/home/flavio.magacho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-user-management/01-CONTEXT.md
@.planning/phases/01-auth-user-management/01-RESEARCH.md
@.planning/phases/01-auth-user-management/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared layout, sidebar navigation, navbar with Google avatar, and dashboard page</name>
  <files>
    src/main/resources/templates/layout/default.html
    src/main/resources/templates/fragments/sidebar.html
    src/main/resources/templates/fragments/navbar.html
    src/main/resources/templates/dashboard.html
    src/main/resources/static/css/custom.css
    src/main/java/com/bemobi/aiusercontrol/web/DashboardController.java
    src/main/java/com/bemobi/aiusercontrol/config/WebConfig.java
    src/main/resources/templates/index.html
  </files>
  <action>
    **1. Create layout/default.html (Thymeleaf Layout Dialect base template):**

    Use Thymeleaf Layout Dialect (`layout:fragment="content"`) for decorator pattern. Structure:
    ```html
    <!DOCTYPE html>
    <html xmlns:th="http://www.thymeleaf.org"
          xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
          xmlns:sec="http://www.thymeleaf.org/extras/spring-security">
    ```
    - `<head>`: Tailwind CSS via CDN (`<script src="https://cdn.tailwindcss.com"></script>`), HTMX via WebJars (`th:src="@{/webjars/htmx.org/dist/htmx.min.js}"`), link to custom.css, `<meta name="_csrf" th:content="${_csrf.token}">`, `<meta name="_csrf_header" th:content="${_csrf.headerName}">`
    - `<body class="bg-gray-50">`: Flex container (`flex h-screen`):
      - Left: `th:replace="~{fragments/sidebar :: sidebar}"`
      - Right: Flex column (`flex-1 flex flex-col overflow-hidden`):
        - Top: `th:replace="~{fragments/navbar :: navbar}"`
        - Main: `<main class="flex-1 overflow-y-auto p-6"><div layout:fragment="content"></div></main>`
    - Desktop only: min-width assumption, no responsive breakpoints

    **2. Create fragments/sidebar.html:**

    Left sidebar fragment (`th:fragment="sidebar"`). Clean/minimal Linear aesthetic:
    - Fixed width (~64px collapsed or ~240px expanded, Claude's discretion)
    - App logo/name at top: "AI User Control"
    - Navigation items (vertical list with icons as simple SVG or text):
      - Dashboard (link to `/dashboard`) -- active when on dashboard
      - Users (link to `/users`) -- active when on users
      - AI Tools (link to `/ai-tools`) -- active when on ai-tools
      - Future phases will add more items (keep structure extensible)
    - Active state: highlighted background for current page (use `th:classappend` with Thymeleaf to detect current URL)
    - Style: `bg-gray-900 text-white` or similar dark sidebar, clean icons, `text-sm` font
    - Desktop only, no hamburger menu

    **3. Create fragments/navbar.html:**

    Top navbar fragment (`th:fragment="navbar"`):
    - Right-aligned user info section showing:
      - Google profile avatar (`<img>` with `th:src` from authenticated principal's picture claim). Use `sec:authentication="principal"` to access the OidcUser. The picture URL is available via `principal.picture` or `principal.getAttribute('picture')`.
      - User name from principal (`principal.fullName` or `principal.name`)
      - Logout button/link (POST to `/logout` with CSRF token, or simple link if using GET logout)
    - Left side: Page title area (can be filled by child templates or left as breadcrumb area)
    - Style: `bg-white border-b border-gray-200`, clean minimal

    **4. Replace dashboard.html:**

    Page extending layout (`layout:decorate="~{layout/default}"`):
    - Title: "Dashboard"
    - Content: Welcome message with empty state since no data exists yet
    - Show: "Welcome to AI User Control" heading
    - Brief stats area (placeholders for future: "Total Users: 0", "AI Tools: 0", "Active Accounts: 0")
    - Empty state card: "Configure AI tools to start monitoring usage. Users will be discovered automatically from provider APIs."
    - Link to AI Tools page as call-to-action

    **5. Replace index.html:**

    Redirect to `/dashboard` (or simply make DashboardController handle `/` -> redirect to `/dashboard`).

    **6. Create DashboardController.java:**

    `@Controller` in `com.bemobi.aiusercontrol.web` package:
    - `@GetMapping("/")` -> redirect to `/dashboard`
    - `@GetMapping("/dashboard")` -> return "dashboard" view
    - `@GetMapping("/login")` -> return "login" view (only accessible when not authenticated)

    **7. Update WebConfig.java:**

    Ensure static resource handling is configured for WebJars path resolution (if not already handled by Spring Boot auto-config). Add any custom view resolvers if needed.

    **8. Create static/css/custom.css:**

    Minimal custom CSS beyond Tailwind. Include:
    - Active sidebar item styles (if complex Tailwind utilities are insufficient)
    - Any transition animations for HTMX swaps (optional, Claude's discretion)
    - Keep it minimal -- Tailwind does the heavy lifting
  </action>
  <verify>
    <automated>cd /home/flavio.magacho/Dropbox/bemobi/dev/ai-user-control-gsd && mvn compile -DskipTests -q 2>&1 | tail -5</automated>
    <manual>Verify layout/default.html has sidebar + navbar + content area. Sidebar has Dashboard/Users/AI Tools links. Navbar displays authenticated user info. Dashboard page extends layout.</manual>
  </verify>
  <done>
    Shared layout template with sidebar navigation (Dashboard, Users, AI Tools) and top navbar (Google avatar, name, logout) is functional. Dashboard page renders with empty state and call-to-action. All pages share the same layout via Thymeleaf Layout Dialect. DashboardController routes / to /dashboard and serves login page. Desktop-only, clean Linear/Vercel aesthetic with Tailwind CSS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement read-only user list and AI Tool CRUD with HTMX</name>
  <files>
    src/main/resources/templates/users/list.html
    src/main/resources/templates/users/fragments/table.html
    src/main/resources/templates/ai-tools/list.html
    src/main/resources/templates/ai-tools/form.html
    src/main/resources/templates/ai-tools/fragments/table.html
    src/main/resources/templates/ai-tools/fragments/form-modal.html
    src/main/java/com/bemobi/aiusercontrol/user/controller/UserController.java
    src/main/java/com/bemobi/aiusercontrol/user/service/UserService.java
    src/main/java/com/bemobi/aiusercontrol/aitool/controller/AIToolController.java
    src/main/java/com/bemobi/aiusercontrol/aitool/service/AIToolService.java
    src/main/java/com/bemobi/aiusercontrol/dto/request/AIToolRequest.java
    src/main/java/com/bemobi/aiusercontrol/dto/response/AIToolResponse.java
    src/main/java/com/bemobi/aiusercontrol/dto/response/UserResponse.java
  </files>
  <action>
    **1. Create UserResponse DTO (replace stub):**

    `UserResponse.java`: Fields: id, email, name, department, avatarUrl, status (String), lastLoginAt, createdAt. Use Lombok `@Data @Builder @NoArgsConstructor @AllArgsConstructor`.

    **2. Create UserService (replace stub):**

    `@Service @RequiredArgsConstructor` with `UserRepository`:
    - `List<UserResponse> findAll()`: Fetch all users, map to UserResponse DTOs
    - `Optional<UserResponse> findById(Long id)`: Fetch single user
    - `long count()`: Count total users
    - No create/update/delete methods -- read-only per CONTEXT.md (users come from provider APIs, not manual creation)

    **3. Create UserController (replace stub):**

    `@Controller @RequestMapping("/users") @RequiredArgsConstructor`:
    - `@GetMapping` -> return "users/list" with model attribute "users" from UserService.findAll()
    - `@GetMapping("/table") @HxRequest` -> return "users/fragments/table :: userTable" for HTMX partial updates
    - NO create/update/delete endpoints -- read-only

    **4. Create users/list.html:**

    Page extending layout (`layout:decorate="~{layout/default}"`):
    - Title: "Users"
    - Heading: "Users" with count badge
    - HTMX-loaded table: `<div id="user-table" hx-get="/users/table" hx-trigger="load" hx-swap="innerHTML">`
    - Loading skeleton placeholder

    **5. Create users/fragments/table.html:**

    Fragment (`th:fragment="userTable"`):
    - Data table with columns: Name (with avatar), Email, Department, Status, Last Login
    - Status badge: green for ACTIVE, yellow for INACTIVE, red for OFFBOARDED
    - Empty state: "No users discovered yet. Users will appear here once AI tool integrations are configured and synced." with informational icon (SVG)
    - Clean data table styling with Tailwind (striped rows or alternating backgrounds)
    - No action buttons (read-only)

    **6. Create AIToolRequest DTO (replace stub if exists, or create):**

    `AIToolRequest.java` with Jakarta Validation:
    - `@NotBlank String name`
    - `@NotNull AIToolType toolType`
    - `String description` (optional)
    - `String apiBaseUrl` (optional)
    - `boolean enabled` (default true)
    - `String iconUrl` (optional)

    **7. Create AIToolResponse DTO:**

    `AIToolResponse.java`: Fields: id, name, toolType (String), description, apiBaseUrl, enabled, iconUrl, createdAt, updatedAt. Lombok `@Data @Builder`.

    **8. Create AIToolService (replace stub):**

    `@Service @RequiredArgsConstructor @Transactional` with `AIToolRepository`:
    - `List<AIToolResponse> findAll()`: Fetch all, map to DTOs
    - `Optional<AIToolResponse> findById(Long id)`: Fetch single tool
    - `AIToolResponse create(AIToolRequest request)`: Validate name uniqueness, create entity, save, return DTO
    - `AIToolResponse update(Long id, AIToolRequest request)`: Find by id, update fields, save, return DTO
    - `void delete(Long id)`: Find by id, delete
    - `long count()`: Count total tools
    - Map between entity and DTO manually or with simple constructor (keep it simple, MapStruct optional for now)

    **9. Create AIToolController (replace stub):**

    `@Controller @RequestMapping("/ai-tools") @RequiredArgsConstructor`:
    - `@GetMapping` -> return "ai-tools/list" with "tools" model attribute
    - `@GetMapping("/table") @HxRequest` -> return "ai-tools/fragments/table :: toolTable"
    - `@GetMapping("/new") @HxRequest` -> return "ai-tools/fragments/form-modal :: toolForm" with empty AIToolRequest and toolTypes enum values
    - `@GetMapping("/{id}/edit") @HxRequest` -> return "ai-tools/fragments/form-modal :: toolForm" with populated AIToolRequest
    - `@PostMapping @HxRequest` -> create tool, return HtmxResponse with refreshed table fragment
    - `@PutMapping("/{id}") @HxRequest` -> update tool, return HtmxResponse with refreshed table fragment
    - `@DeleteMapping("/{id}") @HxRequest` -> delete tool, return empty response (HTMX removes the row)
    - Handle validation errors: return form fragment with error messages

    **10. Create ai-tools/list.html:**

    Page extending layout:
    - Title: "AI Tools"
    - Heading: "AI Tools" with "Add AI Tool" button (triggers HTMX modal)
    - HTMX-loaded table: `<div id="tool-table" hx-get="/ai-tools/table" hx-trigger="load" hx-swap="innerHTML">`
    - Modal container: `<div id="modal-container"></div>` for HTMX-loaded forms

    **11. Create ai-tools/fragments/table.html:**

    Fragment (`th:fragment="toolTable"`):
    - Data table with columns: Name, Type, Status (Enabled/Disabled badge), Actions (Edit, Delete)
    - Edit button: `hx-get="/ai-tools/{id}/edit"` targeting `#modal-container`
    - Delete button: `hx-delete="/ai-tools/{id}"` with `hx-confirm="Are you sure?"`, targeting `closest tr` with `hx-swap="outerHTML swap:1s"`
    - Empty state: "No AI tools configured yet. Add your first tool to begin monitoring." with "Add AI Tool" button

    **12. Create ai-tools/fragments/form-modal.html:**

    Fragment (`th:fragment="toolForm"`):
    - Modal overlay with form for creating/editing AI tools
    - Fields: Name (text input), Type (select dropdown with AIToolType values), Description (textarea), API Base URL (text input), Enabled (checkbox/toggle), Icon URL (text input)
    - Form submits via HTMX: `hx-post="/ai-tools"` for create, `hx-put="/ai-tools/{id}"` for edit
    - Target: `#tool-table` to refresh the table after save
    - Close button to dismiss modal
    - Validation error display inline
    - Include CSRF token in form (Thymeleaf handles this with `th:action`)

    **13. Delete old template stubs:**
    - Delete `src/main/resources/templates/users.html` (replaced by users/list.html)
    - Delete `src/main/resources/templates/usage-report.html` (not needed in Phase 1)
  </action>
  <verify>
    <automated>cd /home/flavio.magacho/Dropbox/bemobi/dev/ai-user-control-gsd && mvn compile -DskipTests -q 2>&1 | tail -5</automated>
    <manual>Verify UserController has GET-only endpoints (no POST/PUT/DELETE). AIToolController has full CRUD with @HxRequest. Templates use layout:decorate. AI tool form uses HTMX for submission. User list has no action buttons.</manual>
  </verify>
  <done>
    User list page renders read-only data table with empty state (no create/update/delete buttons). AI Tool catalog has full CRUD: admin can add, edit, and delete tools via HTMX-powered modal forms without full page reloads. Both pages use shared layout with sidebar and navbar. DTOs separate entity layer from presentation. Service layer handles business logic with proper validation. Empty states are informative with clear messaging.
  </done>
</task>

</tasks>

<verification>
1. `mvn compile -DskipTests` succeeds
2. layout/default.html uses Thymeleaf Layout Dialect with sidebar + navbar + content
3. sidebar.html has navigation links for Dashboard, Users, AI Tools
4. navbar.html displays Google avatar and user name from OAuth2 principal
5. users/list.html is read-only (no create/edit/delete buttons) with empty state
6. ai-tools/list.html has CRUD actions with HTMX partial updates
7. AIToolController has @HxRequest annotations on HTMX endpoints
8. UserController has ONLY GET endpoints (read-only)
9. All pages extend the shared layout via layout:decorate
</verification>

<success_criteria>
- All pages render with consistent sidebar + navbar layout
- Google avatar displays in navbar for authenticated admin
- User list shows read-only table with proper empty state
- AI Tool CRUD works: create, read, update, delete via HTMX without full page reload
- Empty states show informative messages with appropriate calls-to-action
- No manual user edit/deactivate UI exists (read-only per CONTEXT.md)
- Application compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-user-management/01-03-SUMMARY.md`
</output>
